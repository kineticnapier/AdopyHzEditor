import { useEffect, useState, type ReactNode } from "react";
import type { AppState, NoteMutationResult } from "./bridge";
import type { BlankWorkspaceOptions, HarmonicDiagramOptions, HarmonicPreview, QuickHzOptions, QuickHzResult, ToolBackendApi } from "./toolsBridge";
type Common={api:ToolBackendApi;onClose():void;onStatus(text:string):void};
function Modal({title,children,onClose,wide}:{title:string;children:ReactNode;onClose():void;wide?:boolean}){return <div className="modal-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><section className={wide?"modal-window wide":"modal-window"}><header className="modal-title"><strong>{title}</strong><button onClick={onClose}>×</button></header>{children}</section></div>}
function Field({label,children}:{label:string;children:ReactNode}){return <label className="dialog-field"><span>{label}</span><div>{children}</div></label>}
function Num({value,min,max,step=1,disabled,onChange}:{value:number;min:number;max:number;step?:number;disabled?:boolean;onChange(v:number):void}){return <input type="number" value={Number.isFinite(value)?value:0} min={min} max={max} step={step} disabled={disabled} onChange={e=>{const v=Number(e.target.value);if(Number.isFinite(v))onChange(v)}}/>}

export function BlankWorkspaceDialog({api,onClose,onStatus,onState}:Common&{onState(state:AppState):void}){const[value,setValue]=useState<BlankWorkspaceOptions|null>(null);useEffect(()=>{void api.get_blank_workspace_defaults().then(setValue)},[api]);async function apply(){if(!value)return;try{const state=await api.apply_blank_workspace(value);onState(state);onStatus("空のワークスペースに切り替えました");onClose()}catch(e){onStatus(String(e))}}return <Modal title="空のワークスペース" onClose={onClose}><div className="dialog-body export-dialog-body">{value&&<><div className="info-card">音声を外して黒い編集領域に切り替えます。既存ノートは保持されます。</div><Field label="長さ"><Num value={value.duration} min={1} max={36000} step={1} onChange={v=>setValue({...value,duration:v})}/><span>秒</span></Field><Field label="最低MIDI"><Num value={value.midiMin} min={0} max={127} onChange={v=>setValue({...value,midiMin:Math.round(v)})}/></Field><Field label="最高MIDI"><Num value={value.midiMax} min={0} max={127} onChange={v=>setValue({...value,midiMax:Math.round(v)})}/></Field></>}</div><div className="dialog-footer"><span/><button onClick={onClose}>キャンセル</button><button className="primary" onClick={()=>void apply()} disabled={!value}>適用</button></div></Modal>}

export function QuickHzDialog({api,onClose,onStatus}:Common){const[opts,setOpts]=useState<QuickHzOptions>({bpm:175,hz:16,startFloor:0,useEndFloor:false,count:16,endFloor:16,addSetSpeed:true,overwrite:false});const[result,setResult]=useState<QuickHzResult|null>(null),[error,setError]=useState(""),[chart,setChart]=useState("");useEffect(()=>{let alive=true;const id=window.setTimeout(()=>void api.calculate_quick_hz(opts).then(x=>{if(alive){setResult(x);setError("")}}).catch(e=>{if(alive){setResult(null);setError(String(e))}}),80);return()=>{alive=false;window.clearTimeout(id)}},[api,opts]);async function chooseChart(){try{const x=await api.choose_quick_hz_chart();onStatus(x.status);if(x.ok&&x.path){setChart(x.path);setOpts(o=>({...o,startFloor:x.tailFloor??o.startFloor,endFloor:Math.max(o.endFloor,(x.tailFloor??0)+Math.max(1,o.count))}))}}catch(e){setError(String(e))}}async function append(){if(!chart){setError("譜面が選択されていません");return}try{const x=await api.append_quick_hz_chart(chart,opts);onStatus(x.status)}catch(e){setError(String(e))}}async function copy(){if(result?.output){await navigator.clipboard.writeText(result.output);onStatus("Quick Hz出力をコピーしました")}}async function saveText(){if(!result?.output)return;const x=await api.save_quick_hz_text(result.output);onStatus(x.status)}return <Modal title="Quick Hz ツール" onClose={onClose} wide><div className="dialog-body quick-hz-layout"><section className="tool-form"><Field label="BPM"><Num value={opts.bpm} min={.001} max={999999} onChange={v=>setOpts({...opts,bpm:v})}/></Field><Field label="周波数"><Num value={opts.hz} min={.001} max={100000} onChange={v=>setOpts({...opts,hz:v})}/><span>Hz</span></Field><Field label="開始床"><Num value={opts.startFloor} min={0} max={10000000} onChange={v=>setOpts({...opts,startFloor:Math.round(v)})}/></Field><Field label="生成範囲"><div className="compact-row"><label><input type="checkbox" checked={opts.useEndFloor} onChange={e=>setOpts({...opts,useEndFloor:e.target.checked})}/>終了床で指定</label>{opts.useEndFloor?<Num value={opts.endFloor} min={0} max={10000000} onChange={v=>setOpts({...opts,endFloor:Math.round(v)})}/>:<Num value={opts.count} min={1} max={10000000} onChange={v=>setOpts({...opts,count:Math.round(v)})}/>}</div></Field><Field label="SetSpeed追加"><input type="checkbox" checked={opts.addSetSpeed} onChange={e=>setOpts({...opts,addSetSpeed:e.target.checked})}/></Field>{result&&<div className="tool-results"><div>間隔<b>{result.info.intervalMs.toFixed(6)} ms</b></div><div>1拍<b>{result.info.beatMs.toFixed(6)} ms</b></div><div>1打あたりの拍<b>{result.info.beatsPerHit.toFixed(9)} ({result.info.beatFractionText})</b></div><div>相対角度<b>{result.info.relativeAngle.toFixed(9)}°</b></div></div>}{error&&<div className="dialog-error inline">{error}</div>}<div className="chart-tools"><button onClick={()=>void chooseChart()}>譜面を選択…</button><span title={chart}>{chart?chart.split(/[\\/]/).pop():"未選択"}</span><label><input type="checkbox" checked={opts.overwrite} onChange={e=>setOpts({...opts,overwrite:e.target.checked})}/>上書き</label><button onClick={()=>void append()} disabled={!chart||!result}>末尾へ追加</button></div></section><section className="quick-hz-output"><textarea value={result?.output??""} readOnly spellCheck={false}/><div className="preview-actions"><button onClick={()=>void copy()} disabled={!result}>コピー</button><button onClick={()=>void saveText()} disabled={!result}>テキスト保存…</button></div></section></div><div className="dialog-footer"><span/><button className="primary" onClick={onClose}>閉じる</button></div></Modal>}

export function HarmonicDiagramDialog({api,selected,onClose,onStatus,onMutation}:Common&{selected:number[];onMutation(result:NoteMutationResult,selection?:number[]):void}){const[opts,setOpts]=useState<HarmonicDiagramOptions|null>(null),[preview,setPreview]=useState<HarmonicPreview|null>(null),[error,setError]=useState("");useEffect(()=>{void api.get_harmonic_diagram_defaults(selected).then(setOpts)},[api,selected]);useEffect(()=>{if(!opts)return;let alive=true;const id=window.setTimeout(()=>void api.preview_harmonic_diagram(opts).then(x=>{if(alive){setPreview(x);setError("")}}).catch(e=>{if(alive){setPreview(null);setError(String(e))}}),100);return()=>{alive=false;window.clearTimeout(id)}},[api,opts]);async function insert(){if(!opts)return;try{const r=await api.insert_harmonic_diagram(opts);onMutation(r,r.indices??[]);onStatus(r.status);onClose()}catch(e){setError(String(e))}}if(!opts)return <Modal title="倍音ダイアグラム" onClose={onClose}><div className="dialog-loading">読み込み中…</div></Modal>;return <Modal title="倍音ダイアグラム" onClose={onClose} wide><div className="dialog-body harmonic-layout"><section className="tool-form"><Field label="基準周波数"><Num value={opts.rootHz} min={.001} max={100000} onChange={v=>setOpts({...opts,rootHz:v})}/><span>Hz</span></Field><Field label="基準倍率"><input value={opts.rootShift} onChange={e=>setOpts({...opts,rootShift:e.target.value})}/></Field><Field label="1次元オフセット"><Num value={opts.base1dOffset} min={-64} max={64} onChange={v=>setOpts({...opts,base1dOffset:Math.round(v)})}/></Field><Field label="倍音列"><input value={opts.harmonics} onChange={e=>setOpts({...opts,harmonics:e.target.value})}/></Field><Field label="時間単位"><select value={opts.timeUnit} onChange={e=>setOpts({...opts,timeUnit:e.target.value as "seconds"|"beats"})}><option value="seconds">秒</option><option value="beats">拍</option></select></Field><Field label="BPM"><Num value={opts.bpm} min={1} max={2000} disabled={opts.timeUnit!=="beats"} onChange={v=>setOpts({...opts,bpm:v})}/></Field><Field label="開始"><Num value={opts.start} min={0} max={36000} step={.25} onChange={v=>setOpts({...opts,start:v})}/></Field><Field label="長さ"><Num value={opts.duration} min={.001} max={36000} step={.25} onChange={v=>setOpts({...opts,duration:v})}/></Field><Field label="EDO"><Num value={opts.edo} min={1} max={999} onChange={v=>setOpts({...opts,edo:Math.round(v)})}/></Field><Field label="番号オフセット"><Num value={opts.offset} min={-999} max={999} onChange={v=>setOpts({...opts,offset:Math.round(v)})}/></Field>{error&&<div className="dialog-error inline">{error}</div>}</section><section className="harmonic-preview">{preview&&<><div className="preview-summary">{preview.startSeconds.toFixed(6)}秒 – {preview.endSeconds.toFixed(6)}秒</div>{preview.rows.map((r,i)=><div className="harmonic-row" key={i}><b>#{r.pitchNumber}</b><span>{r.local}</span><span>{r.ratio}</span><span>{r.hz.toFixed(6)} Hz</span><span>MIDI {r.midi.toFixed(6)}</span></div>)}</>}</section></div><div className="dialog-footer"><span/><button onClick={onClose}>キャンセル</button><button className="primary" onClick={()=>void insert()} disabled={!preview}>挿入</button></div></Modal>}

export function LanguageDialog({onClose}:Common){return <Modal title="言語" onClose={onClose}><div className="dialog-body export-dialog-body"><div className="info-card">Web UIは日本語表示に統一されています。</div></div><div className="dialog-footer"><span/><button className="primary" onClick={onClose}>閉じる</button></div></Modal>}
export function UpdateDialog({api,onClose,onStatus}:Common){const[info,setInfo]=useState<{version:string;text:string;info:string}|null>(null);useEffect(()=>{void api.get_update_info().then(setInfo)},[api]);async function open(){const x=await api.open_releases_page();onStatus(x.ok?"リリースページを開きました":`リリース: ${x.url}`);onClose()}return <Modal title="更新を確認" onClose={onClose}><div className="dialog-body export-dialog-body">{info?<><div className="info-card">現在のバージョン: {info.version}</div><div className="info-card">{info.text}</div><div className="info-card">{info.info}</div></>:<div className="dialog-loading">確認中…</div>}</div><div className="dialog-footer"><span/><button onClick={onClose}>あとで</button><button className="primary" onClick={()=>void open()}>リリースページを開く</button></div></Modal>}
